{
  "name": "love-diary",
  "private": true,
  "scripts": {
    "test": "node --test",
    "test:relationship": "node --test tests/relationship/*.test.js",
    "test:security": "node --test tests/security/*.test.js",
    "check": "node scripts/regression_checks.js && npm test"
  }
}
